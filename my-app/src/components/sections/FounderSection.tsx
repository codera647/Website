// Deprecated: replaced by TeamSection.tsx (the About page now shows the
// full team as React Bits style profile cards instead of a single founder
// bio). This file is unused and not imported anywhere. Safe to delete
// manually; the tools available in this session can create/edit files in
// your synced folder but aren't permitted to delete them.
export {};
