// Superseded by BlogCard (the site now reuses the exact same card in the
// grid and the article sidebar, matching the reference design). Kept as a
// re-export so any existing imports still work.
export { default } from "./BlogCard";
